/**Enunciado: Realiza un programa que tenga una lista con los dos números enteros que tú quieras y los sume, mostrando por pantalla cada uno de los números y el resultado de la suma.
 *
 * @author rperblac
 */
public class E37t1 {

    public static void main(String[] args) {
        byte[] enteros = {12, 37};
        System.out.println(enteros[0] + " + " + enteros[1] + " = " + (enteros[0] + enteros[1]));
    }

}
